using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayersPanel : MonoBehaviour
{

    //public GameObject whoesTurn;
    //public GameObject[] players = new GameObject[4];
    //public GameObject[] bots = new GameObject[3];
    
    //void Start()
    //{
    //    // fix later
    //    for (int i = 0; i < Board.players.Count; i++)
    //    {
    //        players[i].SetActive(true);
    //        if (i != 0 && Players.isBot[i - 1]) bots[i-1].SetActive(true);
    //    }
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    for (int i = 0; i < Board.players.Count; i++)
    //    {
    //        if (Board.whoesTurn == Board.players[i])
    //        {
    //            if (whoesTurn.transform.position != players[i].transform.position)
    //                whoesTurn.transform.position = Vector3.MoveTowards(whoesTurn.transform.position, players[i].transform.position,
    //                    Time.deltaTime * 500f *Board.gameSpeed); 
    //        }
    //    }
        
    //}
}
