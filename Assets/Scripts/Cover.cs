using UnityEngine;


public class Cover : MonoBehaviour
{
    [SerializeField]
    internal SpriteRenderer board;
    // Start is called before the first frame update
    void Start()
    {
        //board.sprite = Manager.Board_Card.Board;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
